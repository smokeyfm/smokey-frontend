# k8 deployment service
apiVersion: v1
kind: Service
metadata:
  name: dna-frontend
  namespace: default
spec:
  selector:
    app: dna-frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000
  type: ClusterIP
